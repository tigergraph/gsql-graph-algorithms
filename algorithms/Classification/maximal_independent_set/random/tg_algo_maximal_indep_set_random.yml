---
# @           : this file
# {x}         : the directory of file x
# &x          : any file which matches pattern x
# **         : any depth of arbitrary directories
# *           : wildcard character
# [x|y...|z]  : file/directory x, y, or z

  algorithm:
    name: Maximal Independent Set (Randomized)
    filename: "tg_maximal_indep_set_random.gsql"
    sha_id: ed6ea869749977cc0f3df71225d7325fb81c9767
    description: "The randomized version of the MIS algorithm does not return the same results every time it runs. This allows you compare the results of different iterations and select the MIS you find the most appropriate."
    version: lib3.0
    include: false
