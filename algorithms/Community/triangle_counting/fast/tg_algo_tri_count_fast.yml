---
# @           : this file
# {x}         : the directory of file x
# &x          : any file which matches pattern x
# **         : any depth of arbitrary directories
# *           : wildcard character
# [x|y...|z]  : file/directory x, y, or z

  algorithm:
    name: Triangle Count (Fast)
    filename: "tg_tri_count_fast.gsql"
    sha_id: ed6ea869749977cc0f3df71225d7325fb81c9767
    description: "The fast version of the Triangle Counting algorithm is faster than the standard version, but uses some additional memory."
    version: lib3.0
    include: false
